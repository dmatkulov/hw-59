import MovieItem from './MovieItem';
import MovieForm from './MovieForm';

const MovieApp = () => {
  return (
    <div>
      <MovieForm/>
      <MovieItem/>
      MovieApp
    </div>
  );
};

export default MovieApp;